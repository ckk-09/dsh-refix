# dsh-refix P4 阶段报告（收尾）— 全部 13/13 AC 通过

日期：2026-09-16　状态：**P0~P4 全部验收通过**

## 全量回归（进程边界即 AC4.2 证据）

每个验收脚本都是全新 Node 进程：插件、知识库、报告全部随进程消亡后从冷启动重建——这正是 AC4.2"重启失忆为预期行为"的运行时证据（知识库是插件闭包内的内存数组，代码无任何持久化调用）。

```
p0.ac.mts => PASS   （AC1.1 + AC5.1）
p1.ac.mts => PASS   （AC1.2 + AC1.3）
p2.ac.mts => PASS   （AC2.1 + AC2.2 + AC3.1~3.4）
p3.ac.mts => PASS   （AC4.1 + 冷启动断言）
结果：4/4 PASS
```

## AC5.2 显式测试（本轮新增）

`ac/ac52.ac.mts`：以 v4 源码构造契约变体（`inventory` → `inventory2`，等价模拟"DSH 升级后方法消失"，走同一探测路径）：

```
AC5.2 SELF-CHECK PASS
contract: { ok: false, missing: ['dynamicCordisRunner.inventory2:missing'] }  ← 差异报告输出 ✅
巡检门控：newSymptoms = []                                                    ← 不执行诊断 ✅
修复拒绝：{ outcome: 'refused', reason: 'contract-incompatible', missing: [...] } ← 不盲目执行修复 ✅
插件仍可启动并报告（只报告不动手）✅
```

适配路径（需求 §4 AC5.2 后半）：差异报告 → DSH 会话根据报告重新 define 适配版 Package → 人审切换。适配载体是不可变版本包，不是运行时网络代码（§7 安全红线）。

## 关键环境确认（部署前提，本轮查证）

1. **已安装 dsh CLI（D:\Node.js\dsh.cmd, v0.1.5-rc.2）直接从 checkout 源码运行**（`node --import tsx/esm apps/cli/src/bin.ts`）→ 验收 bench 与生产零版本偏移。
2. **web profile `patchReload: 'live'`**（apps/cli/src/profile-boot.ts:355-377）：`cordis.patch.yml` 编辑保存即热加载（config-only HMR），**无需重启 dsh web**；新会话即获得 `cordis_*` 工具。
3. web bundle 已挂载 cordis-host-runner 与 browser faces（tool-cordis README 明示），只缺 model-facing 工具行。

## 交付物总览

```
dsh-refix/
  README.md               # 双语使用说明：一句话自检 + 挂载流程 + 部署 + 13/13 AC 矩阵
  versions/refix-v1-p0.js → refix-v4-p3.js   # 宿主半版本演进（plain JS，cordis_define 即用）
  versions/patient-v1.js / patient-v2-broken.js
  ac/bench.mts + p0~p3.ac.mts + ac52.ac.mts  # 验收（真实 runner，零 mock）
  reports/P0~P4.md                           # 阶段报告与踩坑记录
```

## 待用户确认的部署动作（可选，一条配置）

在 `~/.dsh/profiles/web/cordis.patch.yml` 的既有 `insert:` 列表追加两行（保存即热加载）：

```yaml
    - id: tool-cordis
      name: '@deepseek-ai/dsh-tool-cordis'
```

生效后在新会话说"用 cordis_define 定义并运行 dsh-refix，宿主半代码取自 versions/refix-v4-p3.js"即可挂载，然后"调用 refix_report 给我一份自检报告"完成一句话自检。
