node_modules/
*.log
.tmp-scratch/
.DS_Store

# 静态包构建产物（由 deploy/static/build-static.mjs 生成）
#
# 展开目录不入库 —— 它可由下面的 tgz 解出，且含 4 个文件、内容随源码全量重写。
# **但 tgz 必须入库**：它是"一行命令安装"的唯一载体。库里没有这个文件，用户就得
# 逐文件下载 4 个东西，静态包方案相对"动态包 1 文件 + 贴话术"的收益就归零了。
# 安装（push 后即可用）：
#   dsh plugin --profile web add https://raw.githubusercontent.com/<owner>/<repo>/main/deploy/static/dist/dsh-refix-<ver>.tgz
#
# 注意 gitignore 的父目录规则：父目录一旦被忽略，`!子项` 无法把它救回来，
# 所以这里只忽略展开目录 `dsh-refix/`，不忽略 `dist/` 本身。
deploy/static/dist/dsh-refix/
!deploy/static/dist/*.tgz
