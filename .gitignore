node_modules/
*.log
.tmp-scratch/
.DS_Store

# 静态包构建产物（由 deploy/static/build-static.mjs 生成）
#
# 展开目录 `packages/dsh-refix/` **入库**：awesome-dsh-plugin 收录 CI 只从仓库根或
# packages/ · plugins/ · apps/ 子包抓取 package.json 找 `dsh.bundle` manifest，
# 必须让它可被抓取。tgz 同样入库 —— 它是"一行命令安装"的唯一载体。
# 安装（push 后即可用）：
#   dsh plugin --profile web add https://raw.githubusercontent.com/<owner>/<repo>/main/deploy/static/dist/dsh-refix-<ver>.tgz
